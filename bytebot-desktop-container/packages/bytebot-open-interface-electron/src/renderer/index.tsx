import React from 'react';
import ReactDOM from 'react-dom/client';
import BytebotOpenInterface from './components/BytebotOpenInterface';

const root = ReactDOM.createRoot(document.getElementById('root')!);
root.render(
  <React.StrictMode>
    <BytebotOpenInterface />
  </React.StrictMode>
);